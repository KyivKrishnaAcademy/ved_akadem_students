= q.simple_fields_for :answers, answers_by_question_id[q.object.id] do |a|
  .col-xs-12.col-sm-6.vert-offset-bottom-1
    = a.input :person_id, as: :hidden
    = "#{q.object.position}. #{q.object.data[:text][locale]}"
    = a.input :data, collection: q.object.data[:options][locale], as: :radio_buttons, label: false, wrapper: :horizontal_radio_and_checkboxes
